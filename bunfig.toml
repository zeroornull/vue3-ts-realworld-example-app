# Bun is the project runtime and package manager.
# Keep dependencies installed for development and build scripts.
[install]
linker = "isolated"

[test]
pathIgnorePatterns = ["realworld/**", "tests/e2e/**"]
